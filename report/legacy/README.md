# Legacy report snapshots

The files in this directory were generated before synthesis projects moved
under their owning RTL architecture (`rtl/conv*/synthesis/`). They are kept
unchanged for historical comparison and are not inputs to
`scripts/report-all.py`.

Regenerate current tables with:

```bash
/home/tarsio/gaph/fast-convolution-rtl/.venv/bin/python scripts/report-all.py
```
